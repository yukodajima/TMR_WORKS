$(window).on('load', function () {
  jQuery(function ($) {
    const changeTabPref = $('.p-shopList__areaItem').on('click', function () {
      alert('O');
      // $(this).next().slideToggle();
      // $(this).children().toggleClass('open');
    });
  });
});
